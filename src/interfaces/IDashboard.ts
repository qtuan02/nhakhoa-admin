export interface IDashboard {
    title?: string,
    total?: string,
    message?: string
}

export interface IDashboardInvoice {
    total?: string,
    message?: number
}

export interface IDashboardAppoinment {
    total?: number,
    message?: string
}